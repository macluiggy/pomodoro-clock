[build.environment]
  NODE_VERSION = "14"
  NPM_FLAGS = "--prefix=/dev/null"
[build]
  publish = "build"
  command = "CI=false npx pnpm i --store=node_modules/.pnpm-store && npx pnpm run build"


  

###command = "npx pnpm i --store=node_modules/.pnpm-store && npx pnpm run build"###